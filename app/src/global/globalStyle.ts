import { createGlobalStyle } from 'styled-components';

export const ApplicationGlobalStyle = createGlobalStyle`
    body {
        margin: 0;
        padding-top: 50px;
        padding-left: 70px;
    }

    * {
        font-family: Roboto, Arial;
    }
`